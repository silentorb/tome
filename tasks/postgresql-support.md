# PostgreSQL support

Status: open

## Intent

Add PostgreSQL as a supported SQL backend alongside (or instead of) SQLite for the graph cache / store.

## Notes

See also: [tome-db](../docs/features/tome-db.md), [graph-store](../docs/features/graph-store.md). May interact with the independent-SQL-database task.
