# Independent SQL database without the flat-file database

Status: open

## Intent

Support a use case where the SQL database is the primary store, without requiring the Git-tracked flat-file content database.

## Notes

See also: [tome-db](../docs/features/tome-db.md), [graph-store](../docs/features/graph-store.md).
