# tome-http

HTTP **service module** for `tome-server` and the typed HTTP client for browsers/scripts.

Implements `TomeServiceModule` from `tome-service-interfaces`. Owns `/api/...` routes and `createHttpClient`.
