# tome-sequencing-interfaces

Shared sequencing domain types (`SequenceEvent`, `DependsConstraint`, `SequencingProblem`, …). Types only — no resolution algorithm, Tome, or Imp.

Consumed by `tome-sequencing-resolution` and `tome-sequencing`.

Agent notes: [`AGENTS.md`](./AGENTS.md).
