# tome-theme-midnight

**Midnight** is the default dark-first Tome theme: charcoal surfaces, muted borders, and blue link accents. Shared design tokens (`--tome-*`) and cross-surface component CSS used by `tome-editor` and `tome-static-site` (including silentorb-web builds).

Agent notes: [`AGENTS.md`](./AGENTS.md).
